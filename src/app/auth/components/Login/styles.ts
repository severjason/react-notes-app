import styled from 'styled-components';

const LoginStyles = styled.div`
  .grid-container {
    padding-top: 1rem;
  }
  .form-group {
    width: 100%;
    max-width: 300px;
  }
`;

export default LoginStyles;
