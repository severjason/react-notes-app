import styled from 'styled-components';

const SocialLoginStyles = styled.div`
  .title {
    text-align: center;
    margin: 1rem 0;
  }
  .buttons-container {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
    
    button {
      margin: 0 .4rem;
    }
  }
`;

export default SocialLoginStyles;
